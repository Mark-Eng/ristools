% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{ris_to_df}
\alias{ris_to_df}
\title{Convert a record list to a data frame}
\usage{
ris_to_df(x, delimiter = ris_sep())
}
\arguments{
\item{x}{A \code{ris_records} object, as returned by \code{\link[=read_ris]{read_ris()}} with
\code{return_df = FALSE}.}

\item{delimiter}{The string used to join multiple values in one field.
Defaults to \code{\link[=ris_sep]{ris_sep()}}.}
}
\value{
A data frame with a \code{label} column followed by one column per field
found anywhere in \code{x}. Records lacking a field get \code{NA}.
}
\description{
Collapses a \code{ris_records} list into a data frame with one row per record,
joining multi-value fields with \code{delimiter}.
}
\details{
The conversion is lossless and is reversed exactly by \code{\link[=df_to_ris]{df_to_ris()}},
because the default delimiter cannot occur in bibliographic text. Fields
whose names are short (fewer than 3 characters, i.e. raw RIS tags) are
placed after the named fields.
}
\examples{
recs <- structure(
  list(ref_1 = list(
    title = "A study",
    author = c("Smith, J.", "Jones, A.")
  )),
  class = "ris_records"
)
ris_to_df(recs)

}
\seealso{
\code{\link[=df_to_ris]{df_to_ris()}} for the inverse.
}
