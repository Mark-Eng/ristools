% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openalex.R
\name{oa_ris_types}
\alias{oa_ris_types}
\title{How OpenAlex work types map to RIS reference types}
\usage{
oa_ris_types()
}
\value{
A data frame with one row per OpenAlex work type and two columns:
\code{openalex} (the \code{type} value, lower case) and \code{ris} (the RIS reference
type it becomes).
}
\description{
The \code{TY} recode \code{\link[=apply_ris_tags]{apply_ris_tags()}} applies, as a table.
}
\details{
\code{TY} is the one RIS tag with a closed vocabulary, so an OpenAlex \code{type} of
\code{"article"} cannot be written through unchanged - a reader expects \code{JOUR}.
RIS has no separate type for a review, editorial, letter, erratum,
retraction or peer review, all of which appear in a journal, so all six map
to \code{JOUR}.

A \code{type} that is not in this table, or missing, becomes \code{GEN} (the generic
type) and is named in a warning, so that a work type OpenAlex adds later
never blocks a write but never passes unnoticed either.
}
\examples{
oa_ris_types()

# what does a preprint become?
m <- oa_ris_types()
m$ris[m$openalex == "preprint"]

}
\seealso{
\code{\link[=oa_ris_tags]{oa_ris_tags()}} for the column-name mapping.
}
