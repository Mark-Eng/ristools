% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-ris.R
\name{write_ris}
\alias{write_ris}
\title{Write records to a RIS or BibTeX file}
\usage{
write_ris(
  x,
  filename,
  format = "ris",
  dialect = "ovid",
  tag_map = NULL,
  delimiter = ris_sep(),
  order_tags = TRUE,
  journal_from_position = FALSE,
  year_suffix = NULL,
  blank_line = TRUE,
  eol = NULL
)
}
\arguments{
\item{x}{A data frame (one row per record) or a \code{ris_records} object.}

\item{filename}{Path to write to.}

\item{format}{\code{"ris"} (the default) or \code{"bib"}.}

\item{dialect}{\code{"ovid"} (the default) writes \code{T1}/\code{A1}/\code{Y1}/\code{N2}/\code{JF}, the
tags an Ovid or EndNote export uses, so a file read with \code{\link[=read_ris]{read_ris()}} is
written back with the tags it came in with. \code{"generic"} writes
\code{TI}/\code{AU}/\code{PY}/\code{AB}/\code{JO}.}

\item{tag_map}{Optional named list or vector of \code{field = "TAG"} overrides,
which take precedence over the dialect defaults.}

\item{delimiter}{The multi-value delimiter to split on. Defaults to
\code{\link[=ris_sep]{ris_sep()}}. Only applied to data frame input, since a \code{ris_records}
object already holds proper vectors. Split with \code{fixed = TRUE}, so this
is a literal string rather than a regular expression.}

\item{order_tags}{If \code{TRUE} (the default), tags are written in a canonical
order. If \code{FALSE}, fields keep the order they have in \code{x}.}

\item{journal_from_position}{If \code{TRUE}, a \code{journal} field appearing before
the author/year block is written as \code{T3} rather than \code{JF}, on the
assumption that it holds a series or volume title. Defaults to \code{FALSE},
which is correct for \code{\link[=read_ris]{read_ris()}} output: that reader keeps the original
tag as the field name, so position carries no meaning and this would
relabel a correct \code{journal}. Only set this for records from a reader that
discards the source tag.}

\item{year_suffix}{Appended to bare 4-digit \code{Y1}/\code{PY} values. Defaults to
\code{""}, which writes the year exactly as given, so a file read by
\code{\link[=read_ris]{read_ris()}} writes back unchanged. Pass \code{"//"} for the \code{"2020//"} form
that Ovid exports use.}

\item{blank_line}{If \code{TRUE} (the default), add an empty line after each
\code{ER}, which makes the records easy to tell apart when reading the file.
Pass \code{FALSE} to write one record straight after another.}

\item{eol}{Line ending. Defaults to \code{"\\r\\n"} for RIS and \code{"\\n"} for BibTeX.}
}
\value{
The filename, invisibly.
}
\description{
Writes a data frame or \code{ris_records} object out as RIS or BibTeX.
}
\details{
Each record is written as a \code{TY} line, then its fields, then \code{ER}. Fields
are mapped to tags by \code{dialect}, or by \code{tag_map} where given. A field
holding several values becomes one line per value, repeating the tag.

A field with no entry in the tag map is written under its own name if that
name is one of \code{\link[=ris_valid_tags]{ris_valid_tags()}}, which is what lets the raw-tag output of
\code{\link[=read_ris]{read_ris()}} round-trip unchanged. So a column reaches the file if
\code{tag_map} names it, or the dialect maps it, or it is already a valid tag.

Anything else is dropped, with one warning naming the columns:

\if{html}{\out{<div class="sourceCode">}}\preformatted{The following columns are not named with valid RIS tags; they have been
excluded from your RIS file: display_name, referenced_works, is_oa
}\if{html}{\out{</div>}}

This is what makes it safe to write a data frame that did not come from
\code{\link[=read_ris]{read_ris()}} — \code{\link[=oa2df]{oa2df()}} output, a spreadsheet, an API response — since the
columns with no RIS meaning simply do not appear. The check is against a
fixed list rather than a shape pattern, so a plausible-looking but invalid
name such as \code{TITL} or \code{SDG1} is dropped too, and a column called \code{ER} can
no longer emit a record terminator into the middle of a record. Use
\code{tag_map} to force any of those through.

Dropping happens before values are read, so a list column — which would
otherwise be deparsed into the file as \code{list(id = c(...), ...)} — cannot
reach it. A column that holds a list or a nested table is dropped on those
grounds even when its name \emph{is} a valid tag, with its own warning, since a
correct name says nothing about the contents. The \code{label} and \code{filename}
columns that \code{\link[=read_ris]{read_ris()}} adds are never written.

RIS output uses CRLF line endings and no quoting, as RIS consumers expect.
Records are separated by a blank line; see \code{blank_line}. The blank line is
cosmetic — \code{\link[=read_ris]{read_ris()}} ignores it, so it does not affect a round trip.
}
\examples{
df <- data.frame(
  label = "ref_1",
  type = "JOUR",
  title = "A study",
  author = "Smith, J. | Jones, A.",
  year = "2020",
  stringsAsFactors = FALSE
)

f <- tempfile(fileext = ".ris")
write_ris(df, f)
cat(readLines(f), sep = "\n")
unlink(f)

}
\seealso{
\code{\link[=read_ris]{read_ris()}} to read files back in, \code{\link[=ris_valid_tags]{ris_valid_tags()}} for the
tags that may be written, \code{\link[=oa2df]{oa2df()}} to build a data frame from OpenAlex.
}
