% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-ris.R
\name{write_ris}
\alias{write_ris}
\title{Write records to a RIS or BibTeX file}
\usage{
write_ris(
  x,
  filename,
  format = "ris",
  dialect = "ovid",
  tag_map = NULL,
  delimiter = ris_sep(),
  order_tags = TRUE,
  journal_from_position = FALSE,
  year_suffix = NULL,
  blank_line = FALSE,
  eol = NULL
)
}
\arguments{
\item{x}{A data frame (one row per record) or a \code{ris_records} object.}

\item{filename}{Path to write to.}

\item{format}{\code{"ris"} (the default) or \code{"bib"}.}

\item{dialect}{\code{"ovid"} (the default) writes \code{T1}/\code{A1}/\code{Y1}/\code{N2}/\code{JF}, the
tags an Ovid or EndNote export uses, so a file read with \code{\link[=read_ris]{read_ris()}} is
written back with the tags it came in with. \code{"generic"} writes
\code{TI}/\code{AU}/\code{PY}/\code{AB}/\code{JO}.}

\item{tag_map}{Optional named list or vector of \code{field = "TAG"} overrides,
which take precedence over the dialect defaults.}

\item{delimiter}{The multi-value delimiter to split on. Defaults to
\code{\link[=ris_sep]{ris_sep()}}. Only applied to data frame input, since a \code{ris_records}
object already holds proper vectors. Split with \code{fixed = TRUE}, so this
is a literal string rather than a regular expression.}

\item{order_tags}{If \code{TRUE} (the default), tags are written in a canonical
order. If \code{FALSE}, fields keep the order they have in \code{x}.}

\item{journal_from_position}{If \code{TRUE}, a \code{journal} field appearing before
the author/year block is written as \code{T3} rather than \code{JF}, recovering
series and volume titles. Defaults to \code{FALSE}: this is only correct for
records produced by revtools' reader. \code{\link[=read_ris]{read_ris()}} keeps the original tag
as the field name instead, so on its output this would relabel a correct
\code{journal}.}

\item{year_suffix}{Appended to bare 4-digit years. Defaults to \code{"//"} for
the Ovid dialect and \code{""} otherwise.}

\item{blank_line}{If \code{TRUE}, add an empty line after each \code{ER}.}

\item{eol}{Line ending. Defaults to \code{"\\r\\n"} for RIS and \code{"\\n"} for BibTeX.}
}
\value{
The filename, invisibly.
}
\description{
Writes a data frame or \code{ris_records} object out as RIS or BibTeX.
}
\details{
Relative to revtools' \code{write_bibliography()}:
\itemize{
\item Tags are no longer stripped of digits, so \code{M3} and \code{Y2} survive rather
than becoming \code{M} and \code{Y} and being dropped.
\item Fields with no entry in the tag map are passed through if the field name
is itself a RIS tag; anything genuinely unwritable raises one warning
naming the fields.
\item \code{issue} is mapped, single-value and non-numeric page fields are kept, and
records are written with \code{TY} first and \code{ER} last.
\item RIS is written with CRLF line endings and no quoting.
}
}
\examples{
df <- data.frame(
  label = "ref_1",
  type = "JOUR",
  title = "A study",
  author = "Smith, J. | Jones, A.",
  year = "2020",
  stringsAsFactors = FALSE
)

f <- tempfile(fileext = ".ris")
write_ris(df, f)
cat(readLines(f), sep = "\n")
unlink(f)

}
\seealso{
\code{\link[=read_ris]{read_ris()}} to read files back in.
}
