% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openalex.R
\name{oa_ris_tags}
\alias{oa_ris_tags}
\title{How OpenAlex columns map to RIS tags}
\usage{
oa_ris_tags()
}
\value{
A data frame with one row per mapped column and two columns: \code{oa}
(the \code{\link[=oa2risdf]{oa2risdf()}} column name) and \code{ris} (the RIS tag it becomes).
}
\description{
The column-name mapping \code{\link[=oa2ristags]{oa2ristags()}} applies, as a table.
}
\details{
Rows are in the order \code{\link[=oa2ristags]{oa2ristags()}} puts the columns in. Any \code{\link[=oa2risdf]{oa2risdf()}}
column not listed here keeps its OpenAlex name, and is therefore dropped by
\code{\link[=write_ris]{write_ris()}} with a warning.

Some of these mappings change the value as well as the name, because the RIS
tag means something narrower than the OpenAlex field: \code{cited_by_count} is
prefixed \code{"Cited by: "} for the notes tag \code{N1}, and
\code{sustainable_development_goals} is prefixed \code{"SDGs: "} for the user tag
\code{U3}, so neither is mistaken for a plain note. \code{type} is recoded to a RIS
reference type by the table in \code{\link[=oa_ris_types]{oa_ris_types()}}.
}
\examples{
oa_ris_tags()

# which tag do keywords land in?
m <- oa_ris_tags()
m$ris[m$oa == "keywords"]

}
\seealso{
\code{\link[=oa_ris_types]{oa_ris_types()}} for the reference-type recode,
\code{\link[=ris_tag_lookup]{ris_tag_lookup()}} for what each RIS tag conventionally means.
}
