% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-ris.R
\name{read_ris}
\alias{read_ris}
\title{Read a RIS, BibTeX, Medline or Web of Science file}
\usage{
read_ris(filename, return_df = TRUE, delimiter = ris_sep())
}
\arguments{
\item{filename}{Path to a file, or a character vector of paths. Multiple
files are read and combined, with a \code{filename} column recording the
source of each record.}

\item{return_df}{If \code{TRUE} (the default), returns a data frame with one row
per record. If \code{FALSE}, returns a \code{ris_records} object: a list with one
element per record, each a named list of fields.}

\item{delimiter}{The string used to join multiple values in one field when
\code{return_df = TRUE}. Defaults to \code{\link[=ris_sep]{ris_sep()}}.}
}
\value{
A data frame, or a \code{ris_records} list if \code{return_df = FALSE}.
}
\description{
Imports one or more bibliographic files, detecting the format from their
contents.
}
\details{
Format detection is by content, not extension: a file with more \verb{\{} than
\code{" - "} in its first 200 lines is treated as BibTeX, otherwise as RIS. A
RIS-like file containing a \code{PMID} tag is read as Medline; a \code{.ciw} file is
read with Web of Science tags.

This function differs from revtools' \code{read_bibliography()} in several ways
that preserve data it discarded:
\itemize{
\item Multiple values in one field are joined with \code{\link[=ris_sep]{ris_sep()}}, which is
reversible; \code{" and "} is not.
\item Page ranges take their order from the tag (\code{SP}/\code{BP} before \code{EP}) rather
than from \code{sort()}, which reversed ranges with an abbreviated end page
(\code{"419"} + \code{"41"} became \code{"41-419"}). An end page with no start page is
stored as \code{"-218"}.
\item Trailing full stops in titles are kept, so titles ending \code{"U.S."} or
\code{"D.C."} no longer lose their last character.
\item Where several tags map to \code{journal}, the first supplies it and the others
keep their own tag as a field name, rather than being pasted together.
\item Records with no abstract no longer get an empty one.
\item Tags containing a digit (\code{M3}, \code{Y2}, \code{U1}) keep their case.
\item A \code{year} value with no 4-digit year is kept rather than blanked.
}
}
\examples{
f <- tempfile(fileext = ".ris")
writeLines(c(
  "TY  - JOUR",
  "T1  - A study of things",
  "A1  - Smith, John",
  "A1  - Jones, Mary",
  "Y1  - 2020//",
  "ER  - "
), f)

df <- read_ris(f)
df$author

unlink(f)

}
\seealso{
\code{\link[=write_ris]{write_ris()}} to write files back out, \code{\link[=ris_to_df]{ris_to_df()}} and
\code{\link[=df_to_ris]{df_to_ris()}} to convert between the two representations.
}
